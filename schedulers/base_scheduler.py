"""Base scheduler interface.

Will define the common abstract interface/base class that all scheduling
algorithms implement, e.g., a method to select a target GPU for an incoming
inference request based on current cluster state.
"""
